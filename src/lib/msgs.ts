import { ephemeral as eph } from "./utils.js";
import * as e from "./emoji.js";

export const prodNotFound = eph(`${e.warning} Product not found.`);
export const alreadyVer = eph(`${e.question} You are already verified.`);
